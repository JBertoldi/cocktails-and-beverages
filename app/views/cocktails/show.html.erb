<div class='container-big'>
  <div class='container my-5'>
    <h2><%= @cocktail.name %></h2>
    <div class="d-flex justify-content-between">
      <div>
        <%= cl_image_tag @cocktail.photo.key, height: 300, width: 400, crop: :fill, class: 'mt-4' %>
      </div>
      <div>
        <% if notice.present? %>
          <%= notice.html_safe %>
        <% end %>
          <h4 class='my-3'>Ingredients</h4>
          <div class='container-sm my-3'>
            <ul>
              <% @cocktail.doses.each do |dose| %>
                <li>
                  <%= dose.measurement %> <%= dose.ingredient.name %>
                  <%= link_to dose_destroy_path(dose), method: :delete do %>
                <small><i class='fa fa-minus mx-6'></i></small>
                <% end %>
                </li>
              <% end %>
            </ul>

            <%= link_to 'Add Ingredient', new_cocktail_dose_path(@cocktail), class: 'btn btn-outline-dark' %>
            <%= link_to 'Go Back', cocktails_path, class: 'btn btn-outline-dark' %>
          </div>
      </div>
    </div>
  </div>
</div>
